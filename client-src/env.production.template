# Frontend Production Configuration Template
# Copy this file to .env.production and fill in your actual values
# REQUIRED: All values must be set - no fallbacks

# API Base URL (REQUIRED)
# This is the base URL for API calls in production
VITE_API_BASE_URL=https://tech.davinciya.com/api

# Vite Dev Server Proxy Target (REQUIRED for development)
# This is used by Vite's dev server to proxy API requests
VITE_API_PROXY_TARGET=http://localhost:5030
